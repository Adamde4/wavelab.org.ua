/**
 * Налаштування Telegram-бота для форми заявок.
 *
 * ⚠️ ВАЖЛИВО ПРО БЕЗПЕКУ
 * Це статичний сайт (GitHub Pages), тому токен бота нижче видно у вихідному
 * коді сторінки будь-кому. Це нормально для боту, який лише приймає заявки
 * (sendMessage), АЛЕ той, хто побачить токен, теоретично зможе відправляти
 * повідомлення від імені бота. Це прийнятний компроміс для простого
 * лендингу, але якщо потрібна вища безпека — перенесіть відправку заявки
 * на невеликий serverless-проксі (Cloudflare Worker / Netlify Function /
 * Vercel Edge Function), який зберігає токен на сервері, а сайт лише
 * звертається до нього. Тоді достатньо замінити функцію sendLeadToTelegram
 * у js/lead-form.js на виклик вашого проксі-ендпоінта.
 *
 * ЯК ОТРИМАТИ CHAT_ID (куди приходитимуть заявки):
 * 1. Відкрийте бота @wave_site_bot у Telegram і натисніть "Запустити" (/start).
 * 2. Перейдіть у браузері за посиланням (замініть TOKEN на реальний токен):
 *    https://api.telegram.org/botTOKEN/getUpdates
 * 3. У відповіді знайдіть "chat":{"id": ЧИСЛО, ...} — це і є ваш CHAT_ID.
 * 4. Вставте це число нижче замість "YOUR_CHAT_ID".
 *
 * Якщо заявки повинні приходити в груповий чат — додайте бота в групу,
 * напишіть у групі будь-яке повідомлення і візьміть chat.id звідти
 * (для груп id зазвичай від'ємний, напр. -1001234567890).
 */

const TELEGRAM_CONFIG = {
  botToken: "8604036236:AAFheEtKI9AMSyzGU8Cfm_2aw2ui1ni3XzI",
  chatId: "674580331"
};
